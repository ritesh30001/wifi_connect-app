name: wifi_connect_app
description: A cross-platform WiFi Connect App using Flutter.

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.1
  geolocator: ^10.1.0
  wifi_iot: ^0.3.19+2
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
